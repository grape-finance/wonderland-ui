# WalletConnect Cloud project ID (required for WalletConnect in browsers without an injected wallet)
VITE_WALLETCONNECT_PROJECT_ID=

# Contract, token, bond, LP, and oracle addresses are stored per network in
# src/constants/addresses.ts. Run `npm run sync-frontend:mainnet` from the
# wonderland-contract repository after a fresh deployment.
